# Verification: campaign edition 1.2.0

Automated checks in the delivery environment:

- 54 Node tests pass: all seven cases in two clue orders, full sequential campaigns, each of three endings, negative response consequences, profile validation and personalization, replay identity/settings, puzzle gates, real hexadecimal decoding, migration of a save generated by the original delivered build, damaged-save rejection, and removal of the requested punctuation character.
- Static content validation and production build pass.
- All source and script modules pass Node syntax checks.
- Local HTTP serving and private-path exclusions are checked.
- Prebuilt runtime files match source bytes; ZIP extraction integrity is checked.

## Unverified limits

No actual browser binary is installed in the environment. Browser rendering, interactive IndexedDB behavior, and assistive-technology behavior have not been visually or interactively verified. The earlier browser download attempt timed out. Automated engine and serialization tests do not establish browser compatibility. No external player tests or duration measurements have been performed.

## Manual browser release check

1. Launch a fresh workspace. Enter a valid username and confirm both the live prompt and typed-command history use it.
2. Reject empty/invalid names; change the valid name through Settings and reload.
3. Complete all seven cases. Confirm the deduction prompt appears in Brief for later cases and valid alternative clue orders work.
4. Complete the final report and choose each ending through separate finale replays.
5. Export/import a mid-campaign save; verify identity, settings, evidence, history, and progression.
6. Import an original 0.1.0 save; verify migration, username setup, and continuation beyond Halden.
7. Import invalid JSON and unsupported versions without losing active progress.
8. Verify a disruptive response produces the lower report score and the expected ending consequence.
9. Test keyboard-only play, tab arrow keys, larger text, narrow/wide viewports, and contrast.
10. Block local storage; check the save warning and portable export. Use only one active tab.

Run this checklist in the target browsers before a public launch.


Theme verification adds save/export/replay persistence, old-save defaulting, invalid-ID rejection, complete palette application, and computed contrast of at least 4.5:1 for the tested body, muted, terminal, selected-accent, button-label, and warning pairs. This is not a full accessibility audit. Manually switch every theme in Settings, toggle high contrast, reload, and import an older save as part of the browser checklist.


## Uploaded-project extension checks

The uploaded baseline passed 43 tests before editing; this release passes 54. Added test coverage includes actual uploaded-format save migration, pre-response snapshot round trips, note and link rejection boundaries, seven practice exercises, guided next steps, message-read state, and opt-in audio lifecycle with a test double.

Additional manual browser checks:

1. Save, rename, export, load, and delete a named checkpoint. Verify oldest-first retention after adding a thirteenth.
2. Apply a response, finish its report, and restore the automatic checkpoint. Evidence and notes should return; the new response/report should be undone.
3. Type a 2000-character note, leave the field, reload, and verify persistence. Longer input should be blocked.
4. Start linking evidence, change cases, and link new records; no old-case ID should remain selected.
5. Complete a Practice exercise of each type, try wrong/duplicate answers, and reload.
6. Use graph nodes with keyboard and mouse. Confirm scope enforcement still happens in the engine.
7. Mark a message read, reload, and confirm unread counts.
8. Enable effects and ambience independently, adjust volume, switch away from the tab, and return.

Actual browser IndexedDB/audio behavior remains unverified here.

## Version 1.3.0 interface update

The comfort redesign retains the 54-test campaign suite. Syntax validation and the static build pass. See COMFORT-UPDATE.md for the new interface behavior and browser acceptance checklist. Real-browser layout and interaction verification remains pending.
